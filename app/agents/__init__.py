"""
Agentic orchestration layer (LangGraph) — reserved for a later phase.

Phase 0 deliberately ships NO agent graph logic here. This package exists
only so the directory layout promised in Phase 0 (`/agents`) is present
and importable, and so `langgraph` is a frozen dependency ready to be
used once the Decision Engine's multi-step reasoning graph is designed.
"""
